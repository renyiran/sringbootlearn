<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.anthony.springboot.dao.UserDao">
    <resultMap id="userResult" type="com.anthony.springboot.model.UserModel">
        <id column="id" property="id" jdbcType="BIGINT" />
        <result column="account" property="account" jdbcType="VARCHAR" />
        <result column="name" property="name" jdbcType="VARCHAR" />
        <result column="password" property="password" jdbcType="VARCHAR" />
        <result column="create_time" property="createTime" jdbcType="DATE" />
        <result column="update_time" property="updateTime" jdbcType="DATE" />
        <result column="description" property="description" jdbcType="VARCHAR" />
    </resultMap>

    <sql id="baseColumnList" >
        id, account, name, password, create_time, update_time, description
    </sql>
    <select id="queryList" resultMap="userResult">
        SELECT
        <include refid="baseColumnList" />
        FROM  USER
        <where>
            1=1
        </where>
    </select>
    <select id="queryOne" resultMap="userResult" parameterType="java.lang.Long">
        SELECT
        <include refid="baseColumnList" />
        FROM  USER
        <where>
            1=1
            <if test="id != null" >
                AND id = #{id}
            </if>
        </where>
    </select>
</mapper>